import type { CascaderOption } from '@arco-design/web-vue'

export type IOption = {
  label: string,
  value: any
}
export type Iname = {
  name: string,
  number: string,
  department:string
}
export type ICasOption = CascaderOption
